# Shell scripts must stay LF or they break on Linux (bad interpreter).
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf

# Font binaries must not be touched by line-ending conversion.
*.woff2 binary
